// FIXTURE VULNERAVEL — sem deletedAt, sem AuditLog, sem tenantId.
generator client { provider = "prisma-client-js" }
datasource db { provider = "postgresql"; url = env("DATABASE_URL") }

model Order {
  id   String @id @default(uuid())
  name String
}
