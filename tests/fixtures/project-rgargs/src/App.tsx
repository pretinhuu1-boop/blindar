export const App = () => <Route path="/x" />
const q = useQuery(); fetch("/api/users")
export const F = () => <form onSubmit={go}><button>delete</button></form>
const role: string = "admin"
const url = "/checkout"; const pay = paymentIntents
console.log("phi", patient)
